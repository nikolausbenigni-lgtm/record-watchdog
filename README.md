# RecordWatchdog

RecordWatchdog is a public records directory for official business entity, court, incarceration, bankruptcy, and registry resources.

## Tech stack

- React
- Vite
- Tailwind CSS
- Lucide React

## Deploy

Import this repository into Vercel. The default Vite settings should work:

- Build command: `npm run build`
- Output directory: `dist`
